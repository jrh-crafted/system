[core]
  excludesfile = ~/.gitignore

  # Switch when cloning new repositories, based on
  # desired account.
  #
  # sshCommand = ssh -F ~/.ssh/config.github.jrh
  # sshCommand = ssh -F ~/.ssh/config.github.rlc-jrh

[init]
  defaultBranch = master

[push]
  # Push current branch even if you've never pushed it before.
  default = current

[user]
  name = Joshua Reade Harris
  email = joshua.reade.harris@gmail.com
