[core]
  excludesfile = ~/.gitignore

  # Switch when cloning new repositories, based on
  # desired account.
  #
  # sshCommand = ssh -F ~/.ssh/config.github.4jrh
  # sshCommand = ssh -F ~/.ssh/config.github.adaptably

[init]
  defaultBranch = master

[push]
  # Push current branch even if you've never pushed it before.
  default = current

[user]
  name = Joshua Reade Harris
  email = joshu@readeharr.is
